import { action } from "@storybook/addon-actions";
import SearchForm from "../../components/SearchForm.vue";

<Meta title="SearchForm" component={SearchForm} />

# Switcher

<ArgsTable of={SearchForm} />

This is a simple search with some methods.

export const WithMethods = (args) => ({
  components: { SearchForm },
  setup() {
    return { args };
  },
  template: `
      <SearchForm
        defaultSearchType="title"
        @submit="action"
        v-bind="args"
      />
    `,
  methods: { action: action("submit") },
});

<Canvas>
  <Story
    name="WithMethods"
    args={{
      backgroundColor: "#c7c5c5",
    }}
  >
    {WithMethods.bind({})}
  </Story>
</Canvas>
