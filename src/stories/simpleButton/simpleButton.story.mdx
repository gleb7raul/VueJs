import { Meta, ArgsTable, Story, Canvas } from "@storybook/addon-docs/blocks";
import { action } from "@storybook/addon-actions";
import { linkTo } from "@storybook/addon-links";
import SimpleButton from "../../shared/SimpleButton.vue";

<Meta title="MDX / Button" component={SimpleButton} />

# Button

<ArgsTable of={SimpleButton} />

This is a simple button with some text in it.

<Canvas>
  <Story name="With Text">
    {{
      components: { SimpleButton },
      template: '<simple-button @click="action">Hello Button</simple-button>',
      methods: { action: action("clicked") },
    }}
  </Story>
</Canvas>

You can perform some action when the button is clicked.

<Canvas>
  <RButton onClick={linkTo("Button", "With Some Emoji")}>With JSX</RButton>
</Canvas>

You can even have Emoji in the button.

<Canvas>
  <Story name="With Some Emoji">
    {{
      components: { SimpleButton },
      template: "<simple-button>😀 😎 👍 💯</simple-button>",
    }}
  </Story>
</Canvas>
